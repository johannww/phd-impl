apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "fabric-experiments.name" . }}-bootstrap
  namespace: {{ include "fabric-experiments.namespace" . }}
  labels:
    {{- include "fabric-experiments.labels" . | nindent 4 }}
    app.kubernetes.io/component: bootstrap
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        {{- include "fabric-experiments.labels" . | nindent 8 }}
        app.kubernetes.io/component: bootstrap
    spec:
      restartPolicy: Never
      terminationGracePeriodSeconds: 0
      containers:
        - name: bootstrap
          image: "{{ .Values.images.tools.repository }}:{{ .Values.images.tools.tag }}"
          imagePullPolicy: {{ .Values.images.tools.pullPolicy }}
          command: ["/bin/bash", "/scripts/bootstrap.sh"]
          volumeMounts:
            - name: scripts
              mountPath: /scripts
            - name: crypto
              mountPath: /crypto
            - name: configtx
              mountPath: /configtx
            - name: organizations
              mountPath: {{ .Values.operations.workspace }}/organizations
            - name: workspace
              mountPath: {{ .Values.operations.workspace }}
      volumes:
        - name: scripts
          configMap:
            name: {{ include "fabric-experiments.name" . }}-scripts
            defaultMode: 0755
        - name: crypto
          configMap:
            name: {{ include "fabric-experiments.name" . }}-crypto
            defaultMode: 0755
        - name: configtx
          configMap:
            name: {{ include "fabric-experiments.name" . }}-configtx
            defaultMode: 0755
        - name: organizations
          persistentVolumeClaim:
            claimName: {{ include "fabric-experiments.name" . }}-organizations
        - name: workspace
          emptyDir: {}
